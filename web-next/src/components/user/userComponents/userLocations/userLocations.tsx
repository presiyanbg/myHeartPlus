'use client';

type Props = {
    userId: number,
}

const UserLocations = (props: Props) => {
    return (
        <div className=" min-h-96">
            Locations
        </div>
    )

}

export default UserLocations;